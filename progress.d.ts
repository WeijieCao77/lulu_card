/**
 * The types for progress.js, which is plain ESM so that the server can import
 * it too — the rule it holds is a safety property and must exist exactly once.
 */
export declare function progressOf(s: unknown): number
