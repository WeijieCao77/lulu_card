# VCT 赛事抽签系统完整方案

> 文档状态：方案稿，不包含代码实现  
> 目标规则基线：VCT 2026 赛制  
> 完整范围：Kickoff、Stage 1、Masters I、Stage 2、Masters II、Champions  
> 核心目标：让抽签成为会真实决定分组、对手与晋级路径的赛事事件，而不是播放完即失效的动画。

## 一、结论

这个功能值得做，但不能只在现有随机结果外面套一层抽签动画。最合适的方案是增加一个独立的“赛事抽签事件层”，把赛事流程统一改造成：

```text
资格或种子确定
  → 创建抽签事件
  → 按规则生成并锁定合法结果
  → 玩家参与揭签或作出真实选择
  → 正式生成分组、对阵与赛程
  → 在赛事页面永久保存抽签记录
```

完整版本必须覆盖以下赛事节点：

1. Kickoff 上半区首轮及轮空位抽签。
2. Stage 1 Alpha/Omega 分组抽签。
3. Masters 瑞士轮第一轮及后续轮次抽签。
4. Masters 瑞士轮结束后的一号种子选择对手。
5. Stage 2 Alpha/Omega 重抽分组。
6. Champions 四档小组抽签。
7. Champions 小组赛结束后的八强抽签。

各阶段可以按工程顺序分批开发，但不能把其中任何一项定义成可有可无的“远期功能”。只有上述赛事全部接入，赛事抽签系统才算完整交付。

抽签同时分为两种性质：

- **仪式型抽签**：玩家负责揭晓已经由存档随机种子锁定的结果，不能通过刷新或读档反复刷弱组。
- **决策型抽签**：现实规则本身允许队伍选择时，玩家获得真正的选择权。最重要的场景是 Masters 一号种子选择瑞士轮晋级队作为八强对手。

## 二、当前代码现状与差距

### 2.1 现有基础

当前赛事框架已经具备不少可复用能力：

- 有完整赛季日历和赛事阶段。
- 有可复现的存档随机数系统。
- 已经支持循环赛、瑞士轮、GSL 小组、单败及双败模板。
- 赛程页能够显示“对手待定”。
- Bracket 页面能够展示瑞士轮、Champions 小组和双败淘汰赛。
- Masters 和 Champions 会在资格全部确定后动态创建。

因此，本方案不是重写比赛模拟器，而是在赛事生成和比赛模拟之间增加一个正式的抽签状态层。

### 2.2 主要差距

#### Stage 1 与 Stage 2

目前 `setupSeason()` 在赛季第一天便直接创建 Stage 1、Stage 2 全部赛程。两个阶段都是12队大循环，每队打11场，并不是 Alpha/Omega 两组各6队、每队打5场。

这意味着 Stage 分组抽签不能只增加界面，必须把赛程创建延迟到前置赛事结束、种子池确定并完成抽签之后。

#### Kickoff

目前 Kickoff 是一段五轮的短循环赛，再取总积分前四进入双败淘汰，与2026年真实的12队三败淘汰存在结构性差异。

如果只给现有 Kickoff 加抽签动画，抽出来的仍然不是现实意义上的 Kickoff 签表。因此 Kickoff 必须同时替换为三败淘汰模板。

#### Masters

当前 Masters 已有正确的大体结构：四个赛区冠军直进季后赛，四个二号种子和四个三号种子进入八队瑞士轮。

但仍有三处差距：

- 瑞士轮首轮只是按数组首尾配对，没有明确执行“二号种子对三号种子、且不同赛区”。
- 第二、三轮是后台算法直接生成，没有抽签事件和抽签历史。
- 瑞士轮结束后，季后赛对阵按固定顺序生成，没有一号种子随机决定选择顺序并挑选对手。

#### Champions

当前 Champions 小组使用固定轮转公式，虽然能保证每组一支不同赛区队伍，但没有真实四档抽签过程。

小组赛后的八强对阵也采用固定轮转，并不是真正的“四个小组第一抽不同组的小组第二，同时进行半区规避”。

#### 状态与界面

当前 `Competition` 只保存最终的 `groups`、`seeds`、`swissSeeds` 等结果，没有以下信息：

- 抽签事件类型与规则。
- 种子池和剩余签球。
- 已揭晓进度。
- 自动顺延或冲突规避记录。
- 玩家是否已观看。
- 是否存在必须由玩家完成的选对手决策。
- 抽签结果锁定时间与随机键。

因此需要新增正式数据结构，而不是让 React 组件临时计算一次动画。

## 三、真实赛事规则基线

游戏应固定采用一套版本化的 `VCT 2026` 规则集。即使现实赛事以后调整，已经开始的存档也不应在中途改变规则。

建议规则配置采用类似以下概念：

```text
rulesetId: vct-2026
careerYear: 2026, 2027, 2028...
```

后续生涯年份继续沿用该规则集，除非未来专门开发新的规则时代并在新存档中启用。

### 3.1 Kickoff

2026 Kickoff 的现实结构：

- 每个赛区12队参加。
- 使用三败淘汰赛。
- 上一年度进入 Champions 的4支队伍直接轮空到胜者组第二轮。
- 其余8支队伍通过随机抽签进入胜者组第一轮，形成4组对阵。
- 4支轮空队伍也通过抽签落入第二轮对应位置。
- 胜者组、中段组、败者组共同构成完整三败路径。
- 三条路径最终决定3个 Masters 名额；具体顺位由固定赛事模板决定。
- 除三场最终资格赛采用 BO5 外，其余比赛主要为 BO3。

参考资料：

- [Riot：VCT Americas 2026 Kickoff 赛制与随机签表](https://valorantesports.com/en-US/news/vct-americas-2026-kickoff-everything-you-need-to-know)
- [Riot：VCT EMEA 2026 Kickoff Primer](https://valorantesports.com/en-GB/news/vct-emea-2026-kickoff-primer)
- [VCT 2026 官方赛季手册](https://valorantesports.com/en-US/season/115571062868511862/handbook)

#### 游戏中的抽签步骤

1. 展示“Champions 轮空池”4队和“首轮参赛池”8队。
2. 从首轮参赛池逐一揭签，按顺序填入4场胜者组第一轮。
3. 再揭开4支轮空队伍，将其填入胜者组第二轮的固定槽位。
4. 显示完整三败淘汰路径，以及每条路径对应的 Masters 资格。
5. 抽签完成后才生成 Kickoff 首批正式比赛。

#### 玩家参与方式

- 玩家点击签球逐一揭晓。
- 如果玩家队伍在首轮参赛池，其被抽出时镜头和签表位置高亮。
- 如果玩家队伍获得轮空，明确显示“上一年度 Champions 资格带来的首轮轮空”，避免让玩家误以为随机抽到了轮空。
- 本环节没有选对手权，只提供仪式参与。

### 3.2 Stage 1

2026 Stage 1 的标准结构：

- 每个赛区12队。
- 根据 Kickoff 最终名次分为6个种子池，每池2队。
- 两个小组分别为 Alpha 和 Omega。
- 每组从每个种子池获得1队，最终各6队。
- 每组进行组内单循环，每队打5场 BO3。
- 每组前4名进入季后赛。
- 组内名次决定季后赛入口：第一名获得优势位置，第二、三名进入开局轮，第四名从高风险位置开始。
- 季后赛本身是按小组名次落位，不再额外进行纯随机抽签。

参考资料：

- [Riot：2026 VCT EMEA Stage 1 赛制](https://valorantesports.com/en-GB/news/2026-vct-emea-stage-1-format-dates-and-tickets)
- [Riot：VCT 2026 League Handbook](https://valorantesports.com/en-US/season/115571062868511862/handbook)
- [VCT 2026 China Stage 1 规则资料](https://liquipedia.net/valorant/VCT/2026/China_League/Stage_1)

#### 推荐的六档划分

| 种子池 | Kickoff 名次 |
|---|---|
| 第一档 | 第1、2名 |
| 第二档 | 第3、4名 |
| 第三档 | 第5、6名 |
| 第四档 | 第7、8名 |
| 第五档 | 第9、10名 |
| 第六档 | 第11、12名 |

池编号在不同资料中可能按正序或倒序命名。游戏内部应统一按“第一档最强、第六档最弱”保存，界面同时显示对应名次，避免名称造成误解。

#### 游戏中的抽签步骤

1. 从第一档开始，展示同档的两支队伍。
2. 抽出第一支进入 Alpha，另一支自动进入 Omega。
3. 重复六轮，直至两个小组各有6队。
4. 每次落位后实时显示两个小组的平均能力、上届成绩和玩家所在组。
5. 完成后，为 Alpha 和 Omega 分别生成5轮组内单循环赛程。

#### 季后赛处理

Stage 1 季后赛不是自由随机抽签。签表位置必须来自两组名次，采用版本化的固定映射，例如：

- 两组第一名进入优势轮次。
- 第二、三名根据规则进入胜者组开局轮。
- 第四名从败者组或无容错位置开始。

具体映射应作为 `vct-2026` 规则配置的一部分，不要再复用当前通用的 `1v8 / 4v5 / 2v7 / 3v6` 模板。

### 3.3 Masters

2026 Masters 的现实结构：

- 四个赛区各有3个名额，共12队。
- 四个赛区冠军直接进入八强季后赛。
- 四个二号种子和四个三号种子进入八队瑞士轮。
- 瑞士轮两胜晋级、两负淘汰，全部为 BO3。
- 首轮必须是二号种子对三号种子，并规避同赛区对阵。
- 后续轮次按相同战绩分池抽签。
- 第三轮禁止与此前已经交手的队伍重赛。
- 瑞士轮产生4支晋级队，与4个赛区冠军组成八强双败淘汰赛。
- 四个赛区冠军随机决定选择顺序，依次选择一支瑞士轮晋级队作为八强对手。

参考资料：

- [Riot：Masters London 2026 赛事说明](https://valorantesports.com/en-US/news/masters-london-everything-you-need-to-know)
- [Masters Santiago 2026 详细抽签规则](https://liquipedia.net/valorant/VCT/2026/Stage_1/Masters)

#### 瑞士轮第一轮

抽签池：

- A池：四个赛区二号种子。
- B池：四个赛区三号种子。

步骤：

1. 从A池抽出一支二号种子。
2. B池只保留不同赛区的合法对手。
3. 抽出一支三号种子组成对阵。
4. 重复直至形成4场首轮比赛。
5. 如果某次落位会令剩余球队无合法组合，系统必须自动改用另一合法落位，并在抽签记录中说明。

#### 瑞士轮第二轮

- 1-0队伍只与1-0队伍配对。
- 0-1队伍只与0-1队伍配对。
- 2026规则允许后续轮次出现同赛区对阵。
- 抽签仪式可以简化成两组快速揭签。

#### 瑞士轮第三轮

- 剩余1-1队伍互相配对。
- 不允许重复此前已经发生过的对阵。
- 如果玩家仍在瑞士轮中，完整展示；否则允许快速完成并发布结果。

#### 八强选择对手

这是整个抽签系统中最重要的真实策略玩法：

1. 随机抽出四个赛区冠军的选择顺序。
2. 第一顺位从四支瑞士轮晋级队中选择对手。
3. 被选择的对手从候选池移除。
4. 后续冠军依次选择，最后一个冠军自动获得剩余对手。
5. 根据选择结果填入八强双败签表，确保半区结构与后续来源关系正确。

如果玩家是赛区冠军：

- 展示所有尚未被选走的候选队。
- 可查看候选队瑞士轮战绩、战队能力、近期状态、地图倾向和交手记录。
- 玩家亲自选择并永久锁定。
- 可以选择“交给教练组”，由AI根据综合胜率选择对手。

如果玩家是瑞士轮晋级队：

- 玩家作为被选择方观看整个过程。
- 每次冠军选择时保留悬念，直到玩家被选中或成为最后剩余队伍。

### 3.4 Stage 2

2026 Stage 2 会对 Stage 1 的 Alpha/Omega 分组重新洗牌，同时保持竞技平衡。

通用的现实抽签逻辑：

- 依据 Stage 1 两个小组各自的最终名次进行配对。
- 建立三个交换池：`#1/#2`、`#3/#4`、`#5/#6`。
- 每个池抽出一个名次档。
- 被抽中的该名次两队交换小组，未抽中的名次两队留在原组。
- 最终仍然形成 Alpha、Omega 两个6队组。
- 两个阶段的战绩不继承。
- 每队再次进行5场组内单循环 BO3。

参考资料：

- [2026 VCT Americas Stage 2 分组抽签说明](https://www.vlr.gg/684521/2026-vct-americas-stage-2-groups-drawn/)
- [Riot：VCT Americas 2026 Stage 2](https://valorantesports.com/en-US/news/vct-americas-2026-stage-2-everything-you-need-to-know)
- [Riot：VCT 2026 League Handbook](https://valorantesports.com/en-US/season/115571062868511862/handbook)

#### 游戏中的抽签步骤

1. 先并排展示 Stage 1 的 Alpha 和 Omega 最终排名。
2. 打开“第一、二名池”，抽出哪一个名次档交换小组。
3. 打开“第三、四名池”，重复一次。
4. 打开“第五、六名池”，重复一次。
5. 使用动画将被抽中的两支队伍交换到对面小组。
6. 展示新的 Stage 2 分组，并生成每组5轮赛程。

这个形式比重新抽12支队伍更好，因为玩家能直接理解“哪些老对手留下、哪些对手交换过来”。

#### 2026 Stage 2 后续赛制依赖

真实2026规则还包含 Challengers 进入 Stage 2 Play-In：

- 每组前2名直接进入季后赛。
- 其余VCT队伍进入 Play-In。
- Challengers 的领先队伍加入 Play-In，争夺剩余季后赛名额。

这部分不是抽签动画本身，但会影响 Stage 2 最终签表。若产品目标是完整复刻2026赛事链，则应与 Stage 2 分组一并纳入赛制重构；不能继续沿用当前独立 Ascension 后再升降级的路径，却在界面上声称是完整2026赛制。

### 3.5 Champions

2026 Champions 的现实结构：

- 四个赛区各有4支队伍，共16队。
- 第一档：四个赛区的 Stage 2 一号种子。
- 第二档：四个赛区的 Stage 2 二号种子。
- 第三档：四个赛区的第一积分晋级队。
- 第四档：四个赛区的第二积分晋级队。
- 每组从四个种子档各获得1队。
- 每组必须同时包含四个不同赛区，不能出现同赛区队伍。
- 每组采用四队GSL双败：第一档对第四档，第二档对第三档。
- 每组前2名进入八强。
- 八强采用双败淘汰。

参考资料：

- [Riot：Champions Shanghai 赛事页](https://valorantesports.com/en-PH/tournament/115576361459045501/overview)
- [Champions 2026 分档及分组规则](https://liquipedia.net/valorant/VCT/2026/Champions)
- [Champions Seoul 官方赛事规则 PDF](https://liquipedia.net/commons/images/e/e3/VCT_Champions_Seoul_2024_Event-Specific_Ruleset.pdf)

#### 小组抽签步骤

1. 第一档队伍随机落入 A、B、C、D 四组。
2. 依次抽取第二、第三、第四档。
3. 每次抽出队伍后，只允许其进入尚未拥有该赛区队伍的小组。
4. 如果直接落位会令余下签无法完成，使用全局合法性检查自动安排到合法小组。
5. 每组完成后立即显示首轮 `1号种子 vs 4号种子`、`2号种子 vs 3号种子`。

#### 八强抽签步骤

1. 建立“小组第一池”和“小组第二池”。
2. 每个小组第一抽取一支不同组的小组第二。
3. 同一小组晋级的两队必须位于不同半区，避免在胜者组决赛之前重赛。
4. 抽签完成后生成八强双败模板。
5. 本环节是仪式型抽签，玩家没有选择对手权。

## 四、完整的赛事抽签节点表

| 时间点 | 事件 | 类型 | 是否可能产生玩家决策 | 结果影响 |
|---|---|---|---|---|
| 季前 | Kickoff 签表抽签 | 仪式型 | 否 | 首轮对阵及轮空队伍位置 |
| Kickoff 结束后 | Stage 1 分组抽签 | 仪式型 | 否 | Alpha/Omega 分组及5轮赛程 |
| Masters 开赛前 | 瑞士轮首轮抽签 | 仪式型 | 否 | 二号种子对不同赛区三号种子 |
| Masters 首轮后 | 瑞士轮第二轮抽签 | 仪式型快速版 | 否 | 1-0池和0-1池对阵 |
| Masters 第二轮后 | 瑞士轮第三轮抽签 | 仪式型快速版 | 否 | 1-1池无重赛对阵 |
| Masters 瑞士轮结束 | 八强选择对手 | 决策型 | 玩家为赛区冠军时有 | 八强对阵和半区 |
| Stage 1 结束后 | Stage 2 重抽分组 | 仪式型 | 否 | 三档交换及新小组赛程 |
| Masters II | 与 Masters I 相同的一整套抽签 | 混合型 | 条件触发 | 瑞士轮和八强路径 |
| Champions 开赛前 | 四档小组抽签 | 仪式型 | 否 | A-D组与GSL首轮 |
| Champions 小组赛后 | 八强抽签 | 仪式型 | 否 | 小组第一对不同组第二及半区 |

## 五、推荐的玩家体验

### 5.1 入口与触发

抽签不需要长期占用一个主导航页面，而应作为赛事节点出现：

- 推进到抽签日时，在总览页弹出“赛事抽签直播”。
- 顶部赛程栏出现“待抽签”而不是凭空出现对手。
- 玩家可以选择“进入抽签”或“快速完成”。
- 未观看的抽签在赛事页面显示红点，稍后仍可查看完整回放式记录。
- 只有真正需要玩家选对手时才必须处理；纯仪式抽签不能阻塞整个生涯。

### 5.2 抽签画面

建议使用单个通用 `DrawCeremony` 界面，根据赛事传入不同规则：

- 顶部：赛事名称、城市、抽签阶段及一句话规则。
- 左侧：当前种子池、剩余队伍和不可选原因。
- 中间：签球或签封、抽取按钮和揭晓动画。
- 右侧：实时更新的 Alpha/Omega 小组、瑞士轮对阵或淘汰赛签表。
- 底部：`抽下一签`、`全部揭晓`、`规则说明`。

移动端一次只展示一个主要动作，避免同时挤入所有种子池和完整签表。

### 5.3 动画节奏

- 单签揭晓控制在约0.4至0.8秒。
- 玩家队伍被抽出时允许略长高亮，但不要超过1.5秒。
- 支持“全部揭晓”，不强迫重复点击十几次。
- 尊重 `prefers-reduced-motion`，减少旋转、闪烁和镜头缩放。
- 不使用假概率、稀有度或抽卡音效；这是赛事仪式，不是卡包系统。

### 5.4 规则解释

每次抽签只显示当前最重要的约束。例如：

- “每组必须各有一支第一至第六档队伍。”
- “Masters 首轮二号种子只会抽到不同赛区的三号种子。”
- “这两队同属中国赛区，因此该签自动顺延至下一合法小组。”
- “Champions 同组出线队必须分处两个半区。”

当自动调整发生时必须解释，不允许玩家看到签球落入A组后又无声跳到B组。

### 5.5 展示优先级

- 玩家所在赛区的 Kickoff、Stage 1、Stage 2：完整仪式。
- Masters I、Masters II：无论玩家是否参赛，都保留完整结果；玩家参赛时主动弹出。
- Champions：完整小组与八强仪式。
- 其他三个赛区的区域抽签：后台自动完成，通过新闻展示结果，避免一个赛季观看四次相同 Stage 抽签。
- 玩家可以在设置中开启“观看全部赛区抽签”，但默认关闭。

## 六、随机结果与真实决策的边界

### 6.1 仪式型抽签必须预先锁定

抽签事件一旦创建，应立即用独立随机键算出完整合法结果并保存。玩家的点击只负责揭晓。

推荐随机键：

```text
draw:{saveSeed}:{year}:{competitionKey}:{phase}
```

示例：

```text
draw:4242:2026:stage1:China:groups
draw:4242:2026:masters1:swiss-r1
draw:4242:2026:champions:groups
```

这样可以保证：

- 开卡、转会、训练等无关操作不会改变签表。
- 刷新和重新载入不会重新抽签。
- 手动观看与快速完成得到同一结果。
- 自动模拟脚本可以稳定复现。

### 6.2 决策型抽签才允许改变结果

玩家只能在现实规则本来就允许队伍选择时改变对阵。目前核心场景是 Masters 八强选对手。

该选择必须：

- 立即写入存档。
- 从候选池永久移除被选队伍。
- 不能通过关闭弹窗撤销。
- 在后续签表和新闻中标记为“由某队选择”。

### 6.3 AI 选择对手

AI一号种子不能永远简单选择纸面评分最低的球队，否则选择过程没有人物感。建议评分模型综合：

- 对手当前战队能力。
- 瑞士轮地图和比赛净胜情况。
- 近期状态。
- 地图池匹配度。
- 历史交手。
- 教练风格或风险偏好。

选择新闻可以给一句解释，例如：“EDG 更希望避开状态火热的 PRX，因此选择了瑞士轮2-1晋级的对手。”

## 七、底层数据结构方案

建议新增独立的抽签领域文件 `src/engine/draw.ts`。

概念数据结构如下：

```ts
type DrawKind =
  | 'kickoff-bracket'
  | 'stage1-groups'
  | 'stage2-reshuffle'
  | 'masters-swiss'
  | 'masters-playoff-pick'
  | 'champions-groups'
  | 'champions-playoffs'

interface DrawEvent {
  id: string
  competitionKey: string
  kind: DrawKind
  year: number
  day: number
  phase?: string
  status: 'ready' | 'revealing' | 'awaiting-choice' | 'complete'
  rulesetId: string
  rngKey: string
  pots: DrawPot[]
  slots: DrawSlot[]
  outcome: DrawOutcome
  revealedCount: number
  watched: boolean
  log: DrawLogEntry[]
  userDecision?: DrawDecision
}
```

`GameState` 建议增加：

```ts
drawEvents: DrawEvent[]
pendingDecisionDrawId?: string
```

`Competition` 建议增加：

```ts
drawEventIds?: string[]
groups?: string[][]
groupNames?: string[]
teamGroup?: Record<string, string>
seedPots?: string[][]
rulesetId?: string
```

### 7.1 为什么抽签记录必须单独保存

- 最终分组只能说明结果，不能还原抽签过程。
- 自动顺延需要留下解释记录。
- Masters 选对手需要区分“随机抽到”和“主动选择”。
- UI才能支持稍后查看和跳过后的结果回顾。
- 测试可以直接验证抽签约束，而不是反推赛程。

## 八、抽签算法设计

### 8.1 不使用简单贪心

“抽到队伍后塞进第一个合法小组”的简单算法可能把合法位置提前用完，导致最后一支队伍无处可去。

推荐两种实现方式：

1. 先枚举或回溯生成所有合法完整结果，再由确定性随机数选择一个。
2. 每次揭签前，枚举所有仍可完成全局合法分配的下一步，只从这些安全步骤中抽取。

对于最多16支队伍的规模，两种方式性能都足够。

### 8.2 约束必须声明化

建议把规则表达成可组合约束：

- `oneFromEachPot`
- `maxOnePerRegion`
- `mustCrossSeedPools`
- `avoidSameRegion`
- `sameRecordOnly`
- `avoidRematch`
- `winnerVsRunnerUp`
- `differentGroup`
- `oppositeBracketHalf`

这样 Stage、Masters 和 Champions 可以共享抽签引擎，而不是各写一套容易漂移的特殊逻辑。

### 8.3 最终校验

每个抽签完成前必须运行校验器：

- 参赛队伍没有缺失或重复。
- 所有槽位均填满。
- 所有赛事约束成立。
- 生成的比赛数量与预期一致。
- 同一个抽签事件不能重复生成赛程。

校验失败时不应尝试带着错误结果继续赛季，应记录错误并用完整合法方案重新构造。

## 九、赛事生成流程调整

### 9.1 新赛季初始化

新赛季只创建：

- 赛事壳。
- 日期窗口。
- Kickoff 种子资格。
- Kickoff 抽签事件。
- Challengers 基础赛程。

不能再提前生成 Stage 1 和 Stage 2 的具体对阵。

### 9.2 Kickoff

```text
读取上一赛季 Champions 参赛队
  → 建立轮空池与首轮池
  → Kickoff 抽签
  → 生成三败淘汰赛首批比赛
  → 按模板逐轮推进
  → 形成完整名次与 Masters 三个种子
```

第一年没有游戏内上一赛季历史时，2025 Champions 资格可以来自世界初始数据；如果初始数据没有明确保存，则使用预设的现实参赛名单或按俱乐部声望确定轮空种子，但必须在规则面板写清依据。

### 9.3 Stage 1

```text
Kickoff 全部名次确定
  → 建立六个名次种子池
  → Stage 1 分组抽签
  → 创建两个独立小组积分榜
  → 分别生成五轮组内循环
  → 两组前四按固定规则进入季后赛
```

### 9.4 Masters

```text
四赛区前三名全部确定
  → 创建 Masters 参赛名单
  → 瑞士轮首轮抽签
  → 第一轮结束后创建第二轮抽签
  → 第二轮结束后创建第三轮抽签
  → 产生四支瑞士轮晋级队
  → 随机决定四个赛区冠军选择顺序
  → 玩家或AI依次选择对手
  → 生成八强双败淘汰赛
```

Masters I 和 Masters II 共用同一套引擎与界面，只替换赛事名称、城市、日期和前置赛事。

### 9.5 Stage 2

```text
Stage 1 小组名次确定
  → 建立三个交换池
  → Stage 2 重抽分组
  → 清空新阶段小组战绩
  → 生成新的五轮组内循环
  → 进入 Play-In 与季后赛路径
```

### 9.6 Champions

```text
四赛区 Stage 2 与积分名额全部确定
  → 建立四个种子池
  → 进行小组抽签并校验赛区规避
  → 生成四个GSL小组首轮
  → 各组决出第一、第二名
  → 创建八强抽签
  → 校验不同组对阵和半区规避
  → 生成八强双败淘汰赛
```

## 十、界面接入方案

### 10.1 新组件

- `src/ui/DrawCeremony.tsx`：通用抽签仪式。
- `src/ui/DrawPots.tsx`：种子池和剩余队伍。
- `src/ui/DrawBoard.tsx`：小组、对阵或签表实时落位。
- `src/ui/OpponentChoice.tsx`：Masters 选择对手。

也可以先把后三者作为 `DrawCeremony` 内部组件，稳定后再拆分。

### 10.2 现有页面改动方向

- `ManagerGame.tsx`：全局检测未观看抽签和待完成决策。
- `Dashboard.tsx`：显示“今晚抽签”“等待选择对手”等赛事事项。
- `Schedule.tsx`：抽签前保持“对手待定”，抽签完成后即时补入赛程。
- `Standings.tsx`：Stage 1/2 改为 Alpha、Omega 两个积分榜。
- `Bracket.tsx`：增加抽签来源说明和完整签表历史入口。
- `Digest.tsx`：在推进摘要中显示抽签结果，但不代替完整抽签界面。

### 10.3 永久记录

每个赛事面板提供“抽签记录”折叠区，包含：

- 当时的种子池。
- 完整抽签顺序。
- 自动顺延原因。
- 玩家或AI的选对手记录。
- 最终小组或签表。

## 十一、推进、跳过与无人值守模拟

### 11.1 纯仪式抽签

纯仪式型结果在事件创建时已经锁定，因此可以立即生成后续赛程。界面上的“未观看”不应阻塞比赛模拟。

在玩家下一场相关比赛前，UI应优先弹出或提示抽签结果；玩家仍可快速完成。

### 11.2 玩家决策

如果玩家是一号种子且轮到选择 Masters 对手：

- `advanceDay` 或快速推进必须停下。
- 返回明确的 `pendingDecision`，由界面打开选择窗口。
- 玩家选择或点击“交给教练组”后才继续生成八强赛程。

### 11.3 自动化脚本

无UI的审计和长跑模拟必须有稳定路径：

- 仪式型抽签自动标记完成。
- AI球队自动选择对手。
- 对人类球队可通过模拟选项 `autoResolveDrawDecisions` 交给教练组。
- 不允许脚本因未观看动画而卡死。

## 十二、旧存档兼容

旧存档不能在赛季中途被强制重抽，因为它们可能已经生成甚至打完部分赛程。

迁移规则建议：

- 已存在 Stage 1/2 赛程的旧赛季继续采用旧格式。
- 已创建 Masters 或 Champions 对阵的旧赛事视为“历史结果已锁定”，补一个已完成的兼容抽签记录即可。
- 新规则从旧存档的下一赛季开始生效。
- 新字段统一提供空数组或默认值，避免读取旧存档时报错。
- 保存版本号需要提升，并增加明确迁移测试。

## 十三、推荐实施顺序

所有阶段均属于最终交付范围。以下顺序只是降低工程风险，不代表删减范围。

### 第一阶段：抽签基础设施

- `DrawEvent` 数据结构。
- 独立确定性随机键。
- 约束求解和最终校验。
- 通用抽签仪式、跳过、历史记录。
- 存档迁移和自动模拟接口。

### 第二阶段：Masters I 与 Masters II

- 瑞士轮首轮跨赛区抽签。
- 第二、三轮战绩池抽签。
- 第三轮无重赛。
- 一号种子随机选择顺序。
- 玩家选择对手和AI选择逻辑。

优先验证 Masters，是因为当前已有瑞士轮和双败模板，最适合先证明抽签框架可靠。

### 第三阶段：Stage 1 与 Stage 2

- 延迟创建区域联赛赛程。
- Stage 1 六档 Alpha/Omega 抽签。
- 双小组积分榜和五轮组内循环。
- 区域季后赛专用模板。
- Stage 2 三池交换式重抽。
- 如采用完整2026链路，同步接入 Challengers Play-In。

### 第四阶段：Champions

- 四档、四赛区约束小组抽签。
- GSL首轮自动落位。
- 小组第一对不同组第二。
- 同组队伍半区规避。

### 第五阶段：Kickoff

- 新建12队三败淘汰模板。
- 上一年度 Champions 四队轮空逻辑。
- 8队首轮和4个轮空槽位抽签。
- 三条淘汰路径和三个 Masters 名额。
- 第一赛季初始种子来源。

Kickoff 放在最后实现是因为它对现有模板改动最大，但它仍然是本方案的必交范围。

## 十四、主要风险与处理

### 14.1 赛制改动超过抽签本身

Stage 1/2 和 Kickoff 的当前底层赛制与2026现实规则不同。必须把这部分当成抽签功能的前置赛制重构，不应为了控制工作量而保留错误底层。

### 14.2 抽签动画过多

两站 Masters 各有多轮抽签，若每轮都用长动画会严重打断赛季。解决方法是区分完整仪式和快速仪式，并根据玩家是否参赛决定默认展示强度。

### 14.3 约束算法产生死路

使用全局合法性回溯，不使用只看当前一步的简单贪心；每个结果落盘前进行完整校验。

### 14.4 玩家读档刷签

仪式型结果在事件创建时预先锁定并写入存档，点击顺序不改变结果。真正的玩家选择立即保存。

### 14.5 fixture ID 与重复生成

抽签事件必须有唯一ID和 `finalized` 状态。生成赛程时检查对应事件是否已经消费，避免读档或重复推进创建两套相同比赛。

### 14.6 规则每年变化

采用版本化规则集。现实VCT后来修改规则时，新增规则配置而不是静默改变旧存档。

## 十五、验收标准

### 15.1 通用抽签系统

- 相同存档种子、年份、赛事和阶段始终生成相同结果。
- 开卡、转会、训练和其他随机行为不会改变赛事签表。
- 观看、跳过、刷新或重新载入不会重抽。
- 每支参赛队恰好出现一次，不缺失、不重复。
- 抽签事件只能生成一次正式分组或赛程。
- 所有自动顺延都有可查看的原因记录。
- “全部揭晓”和逐签揭晓得到完全相同的最终结果。
- 减少动态效果模式下仍能完成全部操作。
- 快速推进和无UI模拟不会卡在纯仪式抽签。

### 15.2 Kickoff

- 每个赛区恰好12队进入 Kickoff。
- 上一年度 Champions 的4队全部获得首轮轮空，其他队不获得该轮空。
- 其余8队随机形成4场首轮比赛，每队出现一次。
- 4支轮空队伍各进入一个合法的胜者组第二轮槽位。
- 三败淘汰模板的所有胜负来源均能正确推进。
- 任何队伍第三次失利后才正式淘汰。
- 三个 Masters 名额和对应种子顺位能够唯一确定。
- 赛事能产生完整、无重复的最终排名。

### 15.3 Stage 1

- 每个赛区12队按 Kickoff 名次形成6个两队种子池。
- Alpha、Omega 各6队。
- 每组从每个种子池恰好获得1队。
- 每支队伍只属于一个小组。
- 每队正好进行5场组内常规赛。
- 不产生任何跨组常规赛。
- 两个小组使用独立积分榜和正确的同分排序。
- 每组前4名进入季后赛。
- 季后赛入口与各自组内名次一致，不再使用12队合并总榜。

### 15.4 Masters I 与 Masters II

- 每届赛事恰好12队，四赛区各3队。
- 四个赛区冠军全部直进季后赛。
- 四个二号种子和四个三号种子全部进入瑞士轮。
- 瑞士轮首轮4场全部为二号种子对三号种子。
- 瑞士轮首轮没有同赛区对阵。
- 第二轮只在1-0池或0-1池内部配对。
- 第三轮只在1-1池内部配对。
- 第三轮没有任何重复对阵。
- 四队以两胜晋级，四队以两负淘汰。
- 四个赛区冠军的选对手顺序由独立随机签决定。
- 每个冠军只选择一次，每支瑞士轮晋级队只被选择一次。
- 玩家是一号种子时必须能亲自选择或委托教练组。
- 选择完成前不会生成错误的八强赛程。
- 八强包含四个赛区冠军和四个瑞士轮晋级队，且无重复。
- 后续双败淘汰能正常产生完整排名和冠军。

### 15.5 Stage 2

- 抽签前正确读取 Stage 1 两个小组各自的1至6名。
- `#1/#2`、`#3/#4`、`#5/#6` 三个交换池各执行一次抽签。
- 每个池恰好有一个名次档交换小组，另一个名次档保留原组。
- 重抽后 Alpha、Omega 仍各6队。
- 每支队伍只属于一个新小组。
- Stage 1 战绩不会带入 Stage 2。
- 每队正好进行5场新的组内循环赛。
- 如果启用完整2026 Play-In，VCT与Challengers进入路径及剩余季后赛名额全部正确。
- Stage 2 最终能够正确产生两个直通 Champions 名额和两个积分名额。

### 15.6 Champions

- 赛事恰好16队，四赛区各4队。
- 四个种子池各4队。
- A、B、C、D 每组恰好4队。
- 每组恰好包含一至四档各1队。
- 每组恰好包含四个不同赛区各1队。
- GSL首轮始终是第一档对第四档、第二档对第三档。
- 每组正确产生第一名和第二名。
- 八强每场都是小组第一对小组第二。
- 八强首轮不会出现同组重赛。
- 同一小组晋级的两队位于不同半区。
- 八强签表中的8支队伍全部唯一。
- 双败淘汰赛能正常生成14场比赛、完整排名和最终冠军。

### 15.7 存档与回归

- 旧存档在当前赛季继续使用既有赛程，不被强制重抽。
- 旧存档从下一赛季开始进入新规则。
- 抽签进行到一半时保存并读取，能恢复相同揭晓进度。
- 玩家选对手前后分别保存，均不会产生候选池重复或赛程重复。
- 现有比赛模拟、奖金、冠军积分、晋级提示、日历和董事会目标没有回归错误。
- 完整自动模拟多个种子和多个赛季时，所有赛事均能按时结束，不出现死锁、空对手或重复比赛。

