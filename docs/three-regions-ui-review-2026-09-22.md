# 三大区前端接入审查（2026-09-22）

## DeepSeek 主体工作

实际调用 deepseek-v4-pro 三轮：源码/断点审计、六文件主要补丁、审核返修。输出保存在忽略目录 `.local-data/deepseek-region-ui-{audit,implement,rework}.json`。未向模型提供截图，因此其样式结论仅为源码推断。

人工审核拒绝首轮把人物原联赛资料一起改写的建议；保留 `card.region` 原始六联赛资料，通过统一 `gameRegionOf` 做游戏分类。返修纠正阈值导入位置、误泛指教练评级、日期、旧赛区兜底。应用时修复 DeepSeek 精确替换元数据中的重复匹配和旧文本版本不一致，保留其生成代码。

## 影响面与实现

- `Dossier.tsx`（真实入口 `LoLCatalog.tsx` 转导出）：普通选手/114 教练下拉只有 LPL、LCK、欧美；筛选统一调用游戏映射。行内和资料详情保留 LEC、LCS、LCP、CBLOL 等真实联赛事实，并在说明解释欧美是游戏合并分类。
- `cards/Filters.tsx`：消费 `SERIES` 与 `GAME_REGION_CN`。共用入口覆盖收藏、交易市场、卡组侧栏、阵容选卡弹窗；市场请求继续交统一服务端过滤逻辑。
- `cards/Packs.tsx`：三区库存/系列与自选优惠继续消费引擎；更新旧六区文案和迁移说明。未修改金币购买报价核对、快速开包、网络未知结果锁。
- `cards/WorldsGallery.tsx`：区域按钮改为三区，对原始彩卡区域做统一映射；保留全部 40 张彩卡、荣誉标签、评分和所有人物资料。更新获取说明及普通选手基础金银铜解释。
- `cards/Odds.tsx`：普通选手基础评分说明引用 `cards.GOLD_AT/SILVER_AT`，区分强化与教练；概率计算仍由 Worker 和引擎提供。
- `data/changelog.ts`：新增面向玩家的三区分类、旧包库存迁移、普通评级说明，不暴露模型或技术术语。

挑战和杯赛本来没有独立赛区筛选，不额外新增筛选。天梯对手和收藏详情保留真实联赛标签。非当前 App/CardMode 入口的旧经理模式页不改写，以免把真实赛事赛区机制错改为收藏分类。旧更新日志保留历史发布记录，新记录说明迁移。

## 样式与验收边界

320/390/768/1440 的源码审查：三大区比旧六区按钮更短；现有 `.row.wrap`、`.worlds-toggle` 的换行及图鉴网格断点可复用。本次不新增 CSS、固定宽高或隐藏溢出。以上仅为代码推断，实际浏览器由主线程验收，未做实体 iOS/Android 测试。

## 验证

统一三区 API 落地后，`npm run build` 已通过（包含 `tsc -b`、Vite、预压缩与服务端打包）；`git diff --check` 通过。普通评级任务仍并行，最终合并后需统一复验。主线程负责浏览器各入口和尺寸验收，本子任务未操作共享浏览器。


## 后续统一评级接入与交换保护

DeepSeek 第四轮生成两处教练解释补丁：引用 `COACH_GOLD_AT/COACH_SILVER_AT`，区分普通选手与教练基础评分。父任务另要求天梯对手展示跟随统一标尺，`Ladder.tsx` 调用 `arenaOpponentRating` 并保留未知 ID 原值 fallback；`check_display_text.ts` 保留人物 ID/姓名解码断言，移除本轮重标定后不再成立的原始评分相等检查，评级由专门回归覆盖。

父任务转交 DeepSeek 市场审计后，新增 `market-api.js` 的 `swapAnswer` 行锁内现行卡定义检查；卡缺失或颜色已不一致时条件关闭交换并调用既有退信方法，只退一次，不进入接受者扣卡/扣体力流程。既有卖单继续按原约定价格交易，不强制清库，不改全服杯引擎归档。

实际额外调用 DeepSeek 三轮进行交换守卫/测试返修。人工审核纠正了返回值契约 `why:'rarity'`、测试发单方/收单方、真实托管与邮件领取、错误 default 导入和夹具字段等问题。主要测试代码由 DeepSeek 生成，小型夹具修正由审核完成。新 `scripts/check_swap_regrade.ts` 用临时 PGlite 数据库与真实 API 发单，模拟发单后现行卡定义变化，无生产数据写入。

已通过：

- `check_swap_regrade.ts`：同色交换正常、并发改色拒绝只退一封、重复接受/领取无新增卡、缺失给出/要求卡拒绝、接受者完整存档无变、原等级退回。
- `check_display_text.ts`：人物 ID 与姓名显示检查。
- `check_open_cup_archive.ts`：归档引擎与旧构建续赛、坏归档拒绝。
- `check_cup_registration.ts`：报名阵容保护、已有杯继续无需新费用或新抽签。

教练及普通评级引擎仍由另一个 DeepSeek 工作单落地；全部完成后需统一最终构建，以上不视为已上线。

最终统一评级与 arenaOpponentRating 落地后，npm run typecheck、npm run build、check_swap_regrade.ts 与 check_display_text.ts 均再次通过；diff --check 通过。该结果不等于部署成功，提交/上线由主线程另行处理。
# 主线程最终验收

- 本地构建成功；主线程逐项运行 `.github/workflows/railway.yml` 中的 24 个检查，全部通过。
- 浏览器实际筛选图鉴九种组合，读到 LPL 25/35/43、LCK 23/42/57、欧美 42/125/285，与引擎统计一致。
- 320、390、768、1440 CSS 像素宽度检查图鉴，页面与主内容均无横向溢出；390 手机尺寸截图及电脑布局人工查看通过。
- 320 宽度检查收藏、卡组、交易市场、名人堂、抽卡页面，无横向溢出；有赛区筛选的页面均为三区。卡组初始页面没有赛区筛选，故不将其计作筛选验证。
- 手机概率弹窗实际完成各包三万包计算，包含欧美包及最新选手/教练分界，弹窗无横向溢出。
- 本轮是桌面浏览器响应式模拟，不代表 iOS / Android 真机全功能验收。未通过浏览器对生产账户购买或改动资产。
